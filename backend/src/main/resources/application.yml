server:
  port: 8080

spring:
  application:
    name: arena-dev-api
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/arena_dev}
    username: ${SPRING_DATASOURCE_USERNAME:arena}
    password: ${SPRING_DATASOURCE_PASSWORD:arena123}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
